# Stack Approaches
* LargestRectangleInHistogram - find index of next smaller element, find index of previous smaller element, multiply their difference with height
* MaximumStack - push pair(value, max) to stack, max=max(value, prevmax)